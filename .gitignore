# Preferências pessoais do Claude Code (não versionar)
.claude/settings.local.json
.claude/worktrees/

# Segredos — NUNCA commitar
.env
.env.*
*.key
*secret*
*token*

# Fotos brutas (biblioteca local — só o otimizado vai para assets/)
imagens/

# Sistema
.DS_Store
